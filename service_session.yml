name: "systemd_monitoring"
root: <%= @settings["working_directory"]%>

startup_window: monitoring
windows:
  - monitoring:
      layout: even-vertical
      panes:
        - journalctl -u example_receiver.service -f
        - journalctl -u example_sender.service -f
